import 'dart:ui';

class StatConfig {
  final String key;
  final String label;
  final Color color;

  const StatConfig({
    required this.key,
    required this.label,
    required this.color,
  });
}
